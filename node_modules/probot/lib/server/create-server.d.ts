import express from "express";
import type { Logger } from "pino";
export declare const createServer: (options: ServerOptions) => express.Application;
export interface ServerOptions {
    webhook: express.Application;
    logger: Logger;
}
